package game.entities;


/**
 * Write a description of interface Idle here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public interface Idle
{
    /**
     * 1. get possible directions it can move to (use canMove())
     * 2. Randomly select a move to make
     * 3. execute that move
     */
    public void actionIdle();   
}
